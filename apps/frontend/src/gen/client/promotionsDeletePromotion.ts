/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import client from '@/client'
import type {
  PromotionsDeletePromotionMutationResponse,
  PromotionsDeletePromotionPathParams,
  PromotionsDeletePromotion422,
} from '../types/PromotionsDeletePromotion.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/client'

function getPromotionsDeletePromotionUrl(promotion_id: PromotionsDeletePromotionPathParams['promotion_id']) {
  return `/api/v1/promotions/${promotion_id}` as const
}

/**
 * @description Delete a promotion (Admin only).Requires admin privileges verified by AdminUser dependency.
 * @summary Delete Promotion
 * {@link /api/v1/promotions/:promotion_id}
 */
export async function promotionsDeletePromotion(
  promotion_id: PromotionsDeletePromotionPathParams['promotion_id'],
  config: Partial<RequestConfig> & { client?: typeof client } = {},
) {
  const { client: request = client, ...requestConfig } = config

  const res = await request<PromotionsDeletePromotionMutationResponse, ResponseErrorConfig<PromotionsDeletePromotion422>, unknown>({
    method: 'DELETE',
    url: getPromotionsDeletePromotionUrl(promotion_id).toString(),
    ...requestConfig,
  })
  return res
}